const NAMESPACE = "xre";

const CONSTANTS = Object.freeze({
    tag: {
        attributes: {
            type: `${NAMESPACE}:type`,
        },
    },
    viewhelper: {
        coreNamespace: "f",
    },
});

export default CONSTANTS;
